<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="job">

	<typeAlias alias="Job" type="com.sample.employee.vo.Job"/>

	<insert id="addJob" parameterClass="Job">
		insert into jobs
		(job_id, job_title, min_salary, max_salary)
		values 
		(#id#, #title#, #minSalary#, #maxSalary#)
	</insert>
	
	<select id="getAllJobs" resultClass="Job">
		select
			job_id		as id,
			job_title	as title,
			min_salary	as minSalary,
			max_salary	as maxSalary
		from
			jobs
	</select>
	
</sqlMap>